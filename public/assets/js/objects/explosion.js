Explosion = {
	/*
	root : null, 
	x : 0, 
	y : 0, 
	radius : 0, 
	
	circleGradient : new Gradient({
	type : 'radial',
	endRadius : 15,
	colorStops : [
	  [ 0.0, "rgba(190,105,90,1)" ],
	  [ 0.25, "rgba(5,30,80,0.4)" ],
	  [ 1, "rgba(10,0,40,0)" ]
	]
	}),
	
	initialize : function(root) {
		this.root = root;
		return this;		
	},
	
	update : function() {
		this.x = PLAYER.x+(SPRITE_WIDTH/2);
		this.y = PLAYER.y+(SPRITE_HEIGHT/2);
		var radius = this.radius;
		
		this.root.stroke(0,100-radius*2);
		this.root.noFill();
		this.root.ellipse(this.x,this.y,radius,radius);
		
		if(this.radius<10){
		  this.radius+=3;
		}else{
		  this.radius++;
		}
	}
	*/
}